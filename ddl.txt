CREATE TABLE EMPREGADO (ID  SERIAL NOT NULL, DTYPE VARCHAR(31), NOME VARCHAR(255), SALARIO FLOAT, DEP_ID INTEGER, EXTRA FLOAT, COMISSAO FLOAT, PRIMARY KEY (ID))
CREATE TABLE DEPARTAMENTO (ID  SERIAL NOT NULL, NOME VARCHAR(255), CHEFE_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE EMPREGADO ADD CONSTRAINT FK_EMPREGADO_DEP_ID FOREIGN KEY (DEP_ID) REFERENCES DEPARTAMENTO (ID)
ALTER TABLE DEPARTAMENTO ADD CONSTRAINT FK_DEPARTAMENTO_CHEFE_ID FOREIGN KEY (CHEFE_ID) REFERENCES EMPREGADO (ID)
